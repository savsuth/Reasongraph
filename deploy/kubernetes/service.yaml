apiVersion: v1
kind: Service
metadata:
  name: knowledge-explorer
  namespace: reasongraph
  labels:
    app.kubernetes.io/name: knowledge-explorer
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: knowledge-explorer
  ports:
    - name: http
      port: 80
      targetPort: http
